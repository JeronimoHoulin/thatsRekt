# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Node (for indexer + frontend)
node_modules/

# Logs
*.log

# Env
.env
.env.local

# Detector — artifacts that shouldn't be committed
detector/dist/
detector/workflow-ids.local.json

# Plans / scratchpads (any tasks/ dir, anywhere in the tree).
tasks/
